name: show info
actions:
  - name: show mem and system
    group: alpha01
    steps:
      - type: command
        commands:
          - free -h
          - uptime
  - name: show user info
    group: alpha02
    steps:
      - type: command
        commands:
          - id
